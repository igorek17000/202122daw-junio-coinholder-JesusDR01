import React from 'react';
import { Styled{{pascalCase name}} } from './{{pascalCase name}}.styled';

export const {{pascalCase name}} = () => {
  return (
    <Styled{{pascalCase name}}>
      {{pascalCase name}}
    </Styled{{pascalCase name}}>
  );
};
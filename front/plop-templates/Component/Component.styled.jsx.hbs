import styled from 'styled-components';

import { Box } from '@mui/material';

export const Styled{{pascalCase name}} = styled(Box)`
    /* max-height:  ${({ matches }) => matches ? '72vh' : '62vh'} ; */
`;
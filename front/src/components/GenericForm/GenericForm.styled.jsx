import styled from 'styled-components';

import { Box } from '@mui/material';
import { Formik } from 'formik';

export const StyledGenericForm = styled(Formik)`
  button[type='submit'] {
    margin: 0 5px;
  }

`;

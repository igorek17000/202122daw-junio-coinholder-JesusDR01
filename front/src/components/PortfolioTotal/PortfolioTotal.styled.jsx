import styled from 'styled-components';

import { Typography } from '@mui/material';

export const StyledPortfolioTotal = styled(Typography)`
    /* max-height:  ${({ matches }) => matches ? '72vh' : '62vh'} ; */
`;
import styled from 'styled-components';

import { Box } from '@mui/material';

export const StyledColorModeToggler = styled(Box)`
    /* max-height:  ${({ matches }) => matches ? '72vh' : '62vh'} ; */
`;
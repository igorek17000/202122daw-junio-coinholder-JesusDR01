import styled from 'styled-components';

import { Modal } from 'antd';

export const StyledSelectFromTokenModal = styled(Modal)`
    width: 450px;
    .ant-modal-body {
        padding: 0;
    }
    
`;
